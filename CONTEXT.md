# 研究方向發想系統 · 術語表

> **這份術語表涵蓋整個系統，不只這個 repo。** 系統橫跨三處：這個 `research-api`
> repo、Zeabur 上的 n8n 實例、以及 `~/.claude/skills` 裡的 12 個 skill。只有這裡有
> 版本控制，所以術語與決策紀錄都放這裡。
>
> 每個詞同時給中文與程式碼裡實際使用的英文識別字，因為兩者都會出現在系統裡。

## 執行單位

**題目 / Topic**：
一次發想的起點，例如「肝硬化與非選擇性乙型阻斷劑」。一個題目會扇出成多個查詢。
_避免_：關鍵字、主題（「主題」在雙軸設計裡另有專指，見下）

**查詢 / Query**（`search_query`）：
送給檢索來源的一個具體字串，加上它的領域路由與角度。是儲存與快取的最小單位。
_避免_：搜尋、search

**檢索 / Search**：
一次查詢實際打到外部來源並取回結果的動作。一個查詢可以被檢索多次（不同時間）。

**專案 / Project**（`project`）：
一個題目連同它的領域框架與限制條件。是所有下游產物的擁有者。

**跑動 / Run**（`run`）：
專案裡的一次階段執行，帶有暫停旗標與 token 預算。**可以不存在** — 見「孤兒檢索」。

**孤兒檢索 / Orphan search**：
`run_id` 為空的查詢。schema 仍允許，但**正常情況下不該出現**：W2 在沒收到 `run_id` 時
會自行建立一個專案與一個 `stage = 'lit_search'` 的跑動。看到孤兒檢索代表有東西繞過了 W2。

## 雙軸檢索

**主題軸 / Topic axis**（`axis_source = 'topic'`）：
針對這個題目本身已知什麼、缺什麼的檢索。

**方法軸 / Method axis**（`axis_source = 'method'`）：
針對這個領域近期多了什麼新做事方法的檢索。由範式包決定要掃描什麼。
⚠️ **W2 v1 沒有做這一軸**，因為範式包來自 W1 而 W1 還不存在，掃錯包產生的是雜訊不是廣度。
所以 v1 的 `axis_source` 全部是 `'topic'`，**產出的方向會全部落在該領域既有的方法框架內**。
看到這裡不要以為雙軸已經完成。

**軸交叉 / Axis crossing**（`axis_source = 'crossed'`）：
主題軸與方法軸的組合。**交叉出來的方向必須把「方法 × 主題」當成一組查詢送進新穎性檢查。**
_避免_：單講「交叉」——系統裡有兩種交叉，見「概念交叉」。W2 v1 只做得到概念交叉。

## 概念與查詢

**概念 / Concept**：
題目裡一個可以獨立對到受控詞彙的東西，例如「環境賀爾蒙」或「肺腺癌」。
**一個真實的研究題目通常是兩到三個概念，不是一個**——這是 W2 的輸入單位。
`環境賀爾蒙與肺腺癌` 整串去查 MeSH 回 0 筆，拆開查兩個都命中。

**概念交叉 / Concept crossing**：
把每個概念展開出的描述詞相乘，組成一組查詢。
`Endocrine Disruptors × Adenocarcinoma of Lung`、`× Lung Neoplasms`（上位）、
`× Adenocarcinoma, Bronchiolo-Alveolar`（下位）。
**廣度來自階層，不是來自字串比對**：精確的概念在 MeSH 裡只有一個描述詞，
只靠字串比對會塌成一個查詢。
_避免_：單講「交叉」，見「軸交叉」。

**查詢語法 / Query dialect**：
同一組概念送給不同來源時各自的寫法。**一套字串走天下的代價實測是 99% 的文獻**：
PubMed 語法 `"X"[MeSH Terms]` 送給 Europe PMC 回 7 篇，該概念實際有 704 篇。
交叉一律用文字，不用嚴格 MeSH 標引——後者實測只回 1 篇，會誤報成「幾乎無人研究」。

**詞彙展開 / Vocabulary expansion**（`project.vocab_expansion`）：
把一個題目變成一組受控詞彙描述詞的動作與結果。生醫類走 MeSH，其他領域走 OpenAlex。
**完全不使用 LLM**——這一層是純 HTTP 與字串處理，不花 token。

**展開漂移 / Expansion drift**：
同一個題目在不同時間展開，拿到不一樣的描述詞。MeSH 每年更新，所以這會發生。
**重跑時要重新展開並與上次比對**，不是沿用舊的：沿用會把第一次的選詞錯誤永遠凍住。

**查詢角度 / Query angle**（`query_angle`）：
這一輪檢索採用的切入方式，用來強制不同輪次不重複。連續三輪空手要換一套術語體系。

## 快取

**論文重用率 / Paper reuse rate**：
本次**跑動**回傳的論文中，**在這次跑動開始之前就已經在庫裡**的比例。
**這是驗收用的數字**（門檻 60%）。基準線是跑動開始那一刻的論文 id 集合，
**不是每個查詢各算一次**——見下。
_避免_：快取命中率（語意含糊，已退役）

**跑動內重疊 / Within-run overlap**：
同一次跑動的多個查詢彼此撈到同一篇論文的比例。這是**診斷數字，不是驗收數字**：
它告訴你這 10 個查詢是不是太像了。第一次跑同一個題目時它就會很高，
拿它當驗收會讓「第二次跑要 > 60%」這個條件失去意義。

**查詢重複率 / Query repeat rate**：
本次送出的查詢中，先前已經跑過的比例。回答的是另一個問題：檢索是不是在原地打轉。
**同一性 = 正規化後的 `query_text` 加上 `domain`**：同一字串在不同領域會路由到不同來源、
拿回不同結果，算成同一次檢索會謊報這個數字。
_避免_：快取命中率

**嘗試結果 / Attempt outcome**：
一次檢索裡每個來源的下場：試過了、回來了、或失敗了（含原因）。
**「用了哪些來源」與「哪些來源真的回來了」是兩件事**，因為 PubMed 對本系統的
出口 IP 是永久封鎖，臨床類檢索永遠會有一個來源失敗。

## 領域框架

**範式包 / Paradigm pack**：
一個領域**怎麼證明一件事是對的**。八個：觀察性、實驗性、計算性、形式性、工程、
計量經濟、詮釋性、測量。
_避免_：領域、學科

**領域模組 / Field module**：
一個領域的**審稿人實際拿哪張清單來對**。疊在範式包上，不取代它。

**盲點 / Blind spot**：
框架明說自己看不到的territory。**必須帶進輸出**：宣稱你沒有的涵蓋率，比承認缺口更糟。

**優點標記 / Strength marker**：
一個領域裡什麼算做得好。實測顯示只給缺陷清單會讓優點識別比不用框架還差。

**定位要求 / Positioning demand**：
審稿人會要求的對手、消融與評估範圍。與效度威脅是兩回事：後者問「結果是不是真的」，
前者問「這個貢獻在既有版圖的位置」。
